export interface RouteData {
    routeURLName: string,
    routeLinkText: string,
    routeTooltipValue?: string,
    isVisible?: boolean,
    isAccesible?: boolean,
    isActive?: boolean,
    isURLParams?: boolean,
    isURLQuery?: boolean,
    description?: string,
    isExternalLink?: boolean,
}